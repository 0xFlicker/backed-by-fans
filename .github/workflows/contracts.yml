name: CI

permissions: {}

on:
  push:
    branches:
      - main
    paths:
      - "contracts/**"
      - "scripts/**"
      - ".gitmodules"
      - ".github/workflows/contracts.yml"
  pull_request:
    paths:
      - "contracts/**"
      - "scripts/**"
      - ".gitmodules"
      - ".github/workflows/contracts.yml"
  workflow_dispatch:

jobs:
  check:
    name: Foundry project
    runs-on: ubuntu-latest
    env:
      FOUNDRY_PROFILE: robinhood
    defaults:
      run:
        working-directory: contracts
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: recursive

      - name: Install Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install pinned tooling dependencies
        working-directory: web
        run: bun install --frozen-lockfile

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: v1.7.1

      - name: Show Forge version
        run: forge --version

      - name: Prepare clean-room provenance tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes ripgrep
          git submodule foreach --recursive 'git fetch --tags --force'

      - name: Check clean-room provenance
        run: ./scripts/check-clean-room.sh

      - name: Run Forge fmt
        run: forge fmt --check src script test

      - name: Run Forge build
        run: |
          manifest="$(bash scripts/build-linked-protocol.sh)"
          echo "BBF_LIBRARY_MAPPING=$(jq -er '.mapping' "$manifest")" >> "$GITHUB_ENV"

      - name: Validate deployment tooling
        run: |
          bash -n ../scripts/verify-local.sh ../scripts/test-web-anvil.sh ../web/scripts/generate-contracts.sh scripts/public-chain-common.sh scripts/create-safe.sh scripts/test-create-safe.sh scripts/deploy-protocol.sh scripts/test-deploy-protocol.sh scripts/manage-payment-tokens.sh scripts/test-manage-payment-tokens.sh
          ./scripts/test-create-safe.sh
          ./scripts/test-deploy-protocol.sh
          ./scripts/test-manage-payment-tokens.sh
          forge test --libraries "$BBF_LIBRARY_MAPPING" --match-path "test/deployment/*.t.sol" --code-size-limit 1000000 --gas-limit 1000000000 -vvv

      - name: Run unit and fuzz tests
        run: forge test --libraries "$BBF_LIBRARY_MAPPING" --no-match-path "test/{invariants,deployment,fork}/*.t.sol" --code-size-limit 1000000 --gas-limit 1000000000 -vvv

      - name: Run stateful invariants
        run: forge test --libraries "$BBF_LIBRARY_MAPPING" --match-path "test/invariants/*.t.sol" --code-size-limit 1000000 --gas-limit 1000000000 -vvv

      - name: Validate full capacity and focused independent histories
        run: |
          BBF_FULL_VESTING_CAPACITY=true forge test --libraries "$BBF_LIBRARY_MAPPING" --match-contract VestingCapacityTest --code-size-limit 1000000 --gas-limit 100000000000 -vv
          python3 test/models/run_vesting_histories.py --histories 16 --actions 100

      - name: Install Slither
        run: pipx install slither-analyzer==0.11.6

      - name: Show Slither version
        run: slither --version

      - name: Run Slither high-severity gate
        run: FOUNDRY_LIBRARIES="$BBF_LIBRARY_MAPPING" slither . --skip-clean --config-file slither.config.json --fail-high
