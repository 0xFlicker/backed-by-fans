# Prior testnet release retired for guarded buybacks

The previous promoted candidate is preserved byte-for-byte in
`candidate-cda6f6ed7493-promoted.json`. Its active broadcast pointer was removed; the identical
historical broadcast remains in `run-1789277138.json` under the protocol broadcast directory.

This is a new immutable contract release. The old candidate must not be resumed
or attached to the new generated interfaces. Prepare the replacement operational
manifest from committed source while preserving the existing testnet Safe,
payment tokens, and deferred protocol-token launch. No public transaction or
website cutover is authorized by this retirement.
