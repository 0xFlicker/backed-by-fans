export default function Loading() {
  return (
    <section aria-live="polite" className="page-shell page-loading">
      <p className="eyebrow">Opening the room</p>
      <h1 className="font-display">Bringing the next view into focus.</h1>
    </section>
  );
}
