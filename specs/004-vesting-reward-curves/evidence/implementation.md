# Implementation evidence

Feature: `004-vesting-reward-curves`. Initial revision: `d67bf93a9bb8eb95f181cb42520e5538d6052b20` on `main`. Work is in place; the initial untracked changes were the feature documents from this conversation. No public deployment, commit or push is authorized by this record.

## Setup — 2026-09-09

- Requirements checklist: 16/16 checked; economics checklist: 40/40 checked. These are requirements-quality results, not implementation evidence.
- No `.specify/extensions.yml`; no implementation hooks are configured.
- Tools: Foundry 1.7.1 (`4072e48705af9d93e3c0f6e29e93b5e9a40caed8`), Bun 1.3.14, Python 3.14.7. Foundry config pins Solidity 0.8.36/Cancun; existing project dependencies remain unchanged.
- Baseline: `FOUNDRY_PROFILE=robinhood forge build --ignore-eip-3860` exited 0 with no changes to compile (cached, not clean deployment-size evidence). Foundry could not persist its optional signature cache outside the workspace; this did not fail the command.
- Baseline: `bun run typecheck` from `web/` exited 0 before contract ABI edits. Binding/consumer migration is deliberately gated at T022–T025; this baseline is not a claim that the old generated ABI reflects the work in progress.
- Ignore configuration checked: existing Foundry outputs, web dependencies/build/test output, ESLint and Prettier exclusions are present. Added Python bytecode/virtualenv exclusions for the independent reference tools. No Docker, Terraform, Helm or package publishing setup is required.

## Evidence boundaries

The working tree is the implementation revision until a later explicitly authorized commit. Record exact commands and results for each checkpoint below. A successful mathematical model does not establish Solidity behavior, gas feasibility, browser accessibility or public-chain execution. All feature tasks remain open until their evidence is recorded and checked individually.

## First arithmetic and one-lot slice

- `python3 contracts/test/models/vesting_reference.py`: five exact-fraction examples passed. Includes 120-token four-way vesting, 22.5/7.5 late arrival, 20/10 suspension, future starts/empty cohorts and cumulative curve partitioning.
- Reusable Solidity fixtures compile using the existing MembershipTestConfig and six-decimal token convention, with an explicit-unit allocation helper for other precisions.
- Red check: the focused Solidity command failed on missing RewardCurve/VestingLedger/harness imports before their implementations existed. No feature assertions were disabled to pass compilation.
- `FOUNDRY_PROFILE=robinhood forge test --match-contract 'RewardCurveTest|VestingLedgerTest' --code-size-limit 1000000 --gas-limit 1000000000`: 11 tests passed, including 256 curve partitions and 256 partial-refund cases. The one-lot test harness transfers real mock tokens, retains fractional earned credit and refunds 90 tokens after prior claims without an owner top-up. Mid-interval scaled rates defer sub-raw-unit amounts until the terminal endpoint; final allocations are exact.
- Initial integration with FactoryAndFees exposed the existing 7.5M tier deployment-gas budget: 7,511,515 gas. Removed runtime revalidation of already constructor-validated immutable curve terms. Re-running the same three suites then passed all 44 tests, including the unchanged deployment-gas limit. This does not yet satisfy deterministic public linking or full clean creation-graph validation (T009/T010).

T001–T008 are complete at their scoped checkpoints. The new creation tuple, immutable curve terms, quote getter, factory event and linked ledger initialization are present. After formatting and adding direct-tier/factory curve validation/readback coverage, the three focused suites pass 45 tests (7 curve, 4 ledger, 34 factory). Purchase cash/share behavior is still the pre-feature implementation until the scheduled T016 replacement; no feature deployment is ready.

## Linked deployment foundation

The initial full graph check failed: factory raw CREATE2 calldata was 95,473 bytes against the unchanged 95,000-byte limit. Routing curve validation and quotes through the existing linked ledger removes duplicated embedded arithmetic. RewardCurve remains a pure internal helper and adds no second deployment dependency.

Clean rebuild: `forge clean`, then `bash scripts/build-linked-protocol.sh` from `contracts/`. This compiled the independent leaf (5 files) and linked consumers (196 files) with Solidity 0.8.36. The leaf artifact and its derived CREATE2 mapping are retained under `out/vesting-leaf/`; consumer compilation does not overwrite them. Library self-address substitution is checked before hashing the actual runtime. Local and fork deployment guards verify that runtime before factory deployment; public release plans put the library first and retain its link in plan/broadcast metadata.

With `BBF_VESTING_LINK=$(jq -r '.mapping' out/vesting-leaf/link-manifest.json)`, the command `FOUNDRY_PROFILE=robinhood forge test --libraries "$BBF_VESTING_LINK" --match-contract 'RewardCurveTest|VestingLedgerTest|FactoryAndFeesTest|DeploymentScriptsTest|VestingLinkingTest' --code-size-limit 1000000 --gas-limit 1000000000 -vv` passed 67 tests. Explicit assertions enforce release size limits despite broader local harness flags. Local fixtures deploy the preserved production leaf through the canonical CREATE2 runtime and check its hash; they do not mock the library runtime.

| Artifact | Base creation bytes | Runtime bytes |
|---|---:|---:|
| Preserved VestingLedger | 4,190 | 4,138 |
| MembershipTier | 40,459 | 34,193 |
| MembershipTierDeployer | 44,224 | 2,564 |
| MembershipFactory | 94,439 | 10,166 |
| ProtocolBuybackVault | 32,915 | 18,213 |
| PonsBuybackExecutor | 13,220 | 10,613 |
| ProtocolBurnRouter | 5,606 | 5,358 |
| OnchainMediaStoreFactory | 9,997 | 9,969 |
| OnchainMetadataRenderer | 52,427 | 52,399 |
| RendererPreviewHarness | 850 | 822 |

Exact factory transaction calldata, including salt and chain-specific constructor arguments: mainnet 94,727 bytes; testnet 94,823 bytes. The latter has only 177 bytes of headroom. Later scheduler integration must replace old inline accounting rather than retain both implementations. The actual deployed tier, immutable code stores and unchanged 7.5M tier creation-gas assertion also pass in FactoryAndFeesTest.

Negative link experiment: compiling `VestingLinkingTest.test_preservedLeafDerivesExactConsumerLinkAndRuntime` with `--libraries src/libraries/VestingLedger.sol:VestingLedger:0x1111111111111111111111111111111111111111` failed with `InvalidVestingLink()` as required. The regular suite separately tests rejection of an existing wrong runtime at the correct derived address. `FOUNDRY_LIBRARIES='["src/libraries/VestingLedger.sol:VestingLedger:0x1111111111111111111111111111111111111111"]' bash scripts/build-linked-protocol.sh` also exited 1 before compilation, reporting that externally configured libraries are rejected. The normal derived mapping was restored after the negative compilation.

Wrapper coverage: `bash scripts/test-deploy-protocol.sh` passed preparation, normal/sparse deployment, immutable journals, partial submission, receipt failure, interrupted signing, dropped/reverted recovery and staged-binding/TOCTOU cases before encountering a stale four-null transaction-count assertion in resume verification. Changed that assertion to five and added explicit recorded-mapping, wrong-library-runtime and environment-override assertions. A focused rerun using the unchanged fixture setup plus the section starting at the final `resume-verify` case through end-of-file passed (`bash /tmp/bbf-wrapper-tail.sh`; generated directly from the checked-in shell test, with only its script-directory resolution made absolute). Thus all wrapper sections have been exercised; a single uninterrupted rerun of the entire final script remains part of the broad final checkpoint. All shell RPC/signer/verification tools in these tests are mocks. This is not public deployment or Blockscout verification evidence.

T009/T010 are complete. First-slice numeric limits remain frozen at C=2^112−1, Q=2^128 and checked uint64 service times. Scheduler expansion begins only after these checks.

## Shared scheduler, index and cancellation primitives

T011 first failed to compile because `VestingLedger.ProcessResult` and the scheduler API did not exist. The production one-lot reserve/accrue/cancel paths were then removed; the original token-custody test fixture now invokes the same shared queue/process/cancellation methods used for the full engine.

T012: the reference model now has independently authored funding/cohort intervals, original referrers, cancellation generations, refund reserves, ownership-directed creator claims and a separate exact scaled-policy replay. Nine Python examples pass, including sparse-versus-every-second replay equality, 22.5/7.5 attribution, suspension/restoration, free gaps, current-owner claims and active-plus-future refunds. It counts actual vector changes separately from processing checkpoints for the published error bound. These examples are not the full history corpus required by T020/T076.

T013–T015: one indexed heap node per member, effective-time START/END integration, endpoint tails, original-referrer lazy rates, permanent-weight suspension, carry preservation, empty-vector reserves and generation-prefix cancellation are implemented. Seven scheduler tests include 256 deterministic-seed-driven heap-order/position histories. Nine distribution tests cover late joins, suspension, frequent claims/processing, equal-total vector replacement, original referral intervals, a 64-lot cancellation gas comparison, maximum C/uint64-time arithmetic and exact scaled conservation.

An additional red test found an incomplete-preview edge case: at an unprocessed END timestamp, the active lot's tail was incorrectly treated as earned by its preview. `_activeEarnedAt` now includes only the per-second rate until END is actually processed; the new test passes. No claim/refund could spend this tail because mutations require complete accounting, but the bounded read needed to agree with its pending state.

After rebuilding the preserved leaf and linked consumers, the focused command above extended with `VestingSchedulerTest|VestingDistributionTest` passes 83 tests across seven suites. The whole creation graph and unchanged deployment-gas assertions still pass. T011–T015 are complete at the library boundary; purchase/claim/refund entry points in MembershipTier are the next integration step. No frontend or public lifecycle completion is claimed yet.

## Authorized deployment split (T081–T085)

The earlier five-transaction measurements are superseded. The two existing STOP-prefixed tier bytecode stores now deploy in their own canonical CREATE2 transactions before the factory. The factory receives their addresses, exact total creation length and reconstructed hash, and creates a factory-bound tier deployer that validates the bytes and pins both runtime hashes immutably. Every creator deployment rechecks those hashes. Neither factory nor deployer embeds tier creation bytecode. Authority, economic initialization and atomic creator publication remain unchanged; there is no setter, upgrade or compatibility constructor.

Address dependencies are resolved in build order: independently compiled ledger → derived ledger address → linked tier bytes → derived A/B store addresses → encoded factory constructor → derived factory address. Factory children bind to `address(this)` during construction. The factory address enters tier code only as a constructor argument at creator publication, so there is no circular dependency. The reviewed compiler artifact supplies the expected creation hash; journals pin that exact input rather than trusting an arbitrary existing store.

The release journal is schema 7 with seven components, exact constructor arguments, payload lengths, source-verification arguments and deterministic identities. Local/fork bootstrap and manifests include ledger, both stores, tier deployer, router and the vault's existing executor bytecode store. The latter stays inside the factory transaction. Verification compares raw code-store bytes exactly (including metadata), validates the library's self-address/runtime and actual consumer link, and checks runtime metadata plus immutable bindings across the complete graph. The creator rehearsal additionally compares its deployed tier runtime and reads back its owner, factory, vault, payment asset and economic settings.

Build and contract checks: `FOUNDRY_TEST=test/deployment bash scripts/build-linked-protocol.sh`, then the linked `DeploymentScriptsTest|VestingLinkingTest` command with the existing Robinhood harness flags: **25 passed, zero failed/skipped**. Each assertion retains the original release limits. The base tier creation is 39,572 bytes and runtime is 33,376. Factory base creation is 55,629 bytes; the deployer is 3,768 creation / 2,808 runtime bytes. Exact chain-specific factory payloads are **56,045 mainnet / 56,141 testnet**, giving at least **38,859 bytes** below 95,000.

`bash contracts/scripts/test-deploy-protocol.sh` (invoked from the repository root via its script path): **passed**. This includes full source-verification promotion, interrupted ledger/store/factory submissions, resume after A and B, conflicting store runtime/immutable binding rejection, old schema rejection and the exact one-byte-over-limit negative case. Its RPC, signer and explorer interactions are mocks; it performs no public deployment.

The loopback-only `bun scripts/rehearse-split-deployment.ts http://127.0.0.1:19847` mined and verified these actual transaction payloads on fresh Anvil chain 31337:

| Transaction | Exact data bytes | Receipt gas |
|---|---:|---:|
| Vesting ledger | 15,687 | 3,434,664 |
| Media store factory | 10,029 | 2,213,477 |
| Renderer | 52,459 | 11,411,670 |
| Preview harness | 882 | 231,950 |
| Tier code A | 20,219 | 4,343,190 |
| Tier code B | 20,219 | 4,341,462 |
| Factory (one local asset, protocol token unbound) | 55,981 | 11,567,984 |
| Creator tier creation | 1,540 | 7,217,075 |

The creator transaction used the unchanged **7,500,000 gas limit**. The compact encoded ledger views retain typed public tier results because ordinary library-return decoding exceeded that separate creator gas gate in a measured experiment (estimated 7,592,938). This is no longer needed to squeeze the factory below its payload limit. The redundant safe-transfer override was removed; standard soulbound behavior remains in `_update`.

Evidence is retained in `contracts/deployments/split-rehearsal/{status,transactions,protocol-graph,created-tier}.json`, including exact payload hex, receipts, source snapshot and compiled/runtime proof. This is mock-token, local-owner, unbound-protocol evidence, not authentic-fork or public-explorer proof. Anvil was stopped. Runtime/source/manifest tests passed **30 tests across three web suites**; the graph-tool typecheck passed before the subsequent new-ABI consumer migration. The lifecycle Python suite passed 12 tests. No public deployment, commit or push occurred.

## Public vesting foundation (T016–T021)

`PublicVestingTest` passes **9 tests** through real locally deployed factory/tier/router/vault code with the deterministic ledger and stores. Coverage includes the 120-token example, zero purchase-time cash, partial refund after claims, exact terminal allocation, current-owner claims, durable member credit after burn, positive-only restoration, preserved free eligibility, permanent curve cursor, incomplete same-time backlog rollback, paused/behind claims and ERC-5643 cancellation. Fractional credits remain protected; a member's raw claim can be one smallest unit below its allocation while its scaled entitlement remains exact.

`python3 contracts/test/models/run_vesting_histories.py --histories 16 --actions 100` passes **1,600 public economic actions at two processing frequencies**, with exact scaled conservation/frequency identity and independently bounded rational attribution. Every curve is exercised in fixed and PWYW modes; all required transitions have nonzero coverage. Inputs, batch hash, logs and failed-seed retention are under `contracts/deployments/vesting-histories/`. A harness-only prank-order bug was fixed before this pass; an external pricing read had consumed the intended member impersonation. This focused corpus closes T020, not the 10,000-history T076 gate.

The migrated router suites pass **19 tests**. They preserve prior buyback source/cooldown/reentrancy coverage and add zero-budget release, release failure after useful accounting, unique-tier/shared-budget validation before mutation and no-work rejection. Exhaustive stage faults and capacity measurements remain in US7. T016–T021 are complete at their public-contract foundation boundary. Generated bindings and all affected web consumers are now being migrated together; full contract/web suites and browser/fork/security acceptance remain open.

## Generated bindings and consumer integration (T022–T025)

The Wagmi CLI remains the only ABI/address generator. Its wrapper now builds linked contracts under the existing generation lock before invoking the Foundry plugin; the plugin does not rebuild them without the link. New creation tuples derive their type from the generated ABI, supply Some defaults and validate canonical boost/horizon conversion. Registry confirmation reads back the actual immutable terms. Tier reads capture accounting status, curve position, settled balances and the reserved-refund object at a common block. Refund controls no longer request owner allowances or top-ups. Payment reconciliation uses the supplied receipt's actual SharesIssued event; claims no longer require a permanently zero refreshed balance.

Every removed tier/router API consumer in the web, scripts and browser fixtures now uses the generated replacement shapes. Shared advance preparation uses bounded tier-global accounting/status reads and a shared 100-step budget; the runner resumes across sweeps without lifetime token-ID scans and retains its existing trading policy. The activity panel distinguishes already earned protocol fees from cancellable funding reserves instead of inventing future payouts from incomplete accounting. Actual advance receipt outcomes distinguish accounting progress, release and purchase failures.

The obsolete local `contracts/broadcast/DeployDirectProtocol.s.sol/46630/run-latest.json` pointer was retired after confirming it was byte-identical to the retained `run-1788972559.json` historical record. It described the previous deployed ABI and cannot be an active address source for this unreleased version. No historical record or onchain deployment was changed; generated new-version factory addresses are empty until a future authorized deployment.

From `web/`, `FOUNDRY_TEST=test/vesting bun run generate:check`, `bun run typecheck`, `bun run lint`, `bun run build` and the full unit suite pass: **603 tests in 94 files**, with zero lint errors/warnings. The focused Foundry test root is temporary while remaining legacy Solidity tests are migrated; it is not a final full-contract gate. New runner and rehearsal tests cover bounded/fair repeated accounting, partial failures, unresolved transaction stopping and exact simulation-request submission. Build/type/unit evidence closes this binding checkpoint only. Browser lifecycle flows, richer curve/claim presentation, shared backlog recovery UX, authentic-fork testing and the full contract/history/security gates remain open in their story tasks.

## US1: public payment and allocation acceptance (T026–T030)

`PaymentsAndTimeTest|VestedAllocationsTest` pass **29 tests**, including 256 runs of each payment-split, frequency and mixed-lot refund fuzz case. The obsolete `ProtocolFeeAccrual.t.sol` suite has been replaced by `VestedAllocations.t.sol`; its collection/refund order, event provenance, free gaps, burnt identity, 100% protocol, pagination, long-history and gas cases remain covered using the four-purpose API. Purchase-time cash is zero, initial dust and unused referrals fund the creator portion, and funded service starts ahead of grants but after previously purchased free time. Bad transfers roll back catch-up, curve position, funding, access and custody. PaymentProcessed includes free access; PaymentAllocated, SharesIssued and funded-lot events occur only for positive payment and report funding/actual weight rather than cash payouts.

All prior measured gas ceilings remain: 100 due member checkpoints cost **12,269,603 < 15M**, aggregate release **120,049 < 200k**, 512-lot refund **230,242 < 350k** and rejoin **249,355 < 450k**. Bounded allocation views cost 56,399 for one lot and 73,392 for 512, below the existing +50k comparison. Tiny raw allocations explicitly test rate rounding, END tails and protected cancellation residue rather than rounding these reserves into earned cash.

`LocalLifecycleEvidenceTest` passes **4 tests**. The 120-token / 12-period / 80-10-5-5 local trace reserves 120,000,000 raw units at purchase. After three periods it pays creator 24,000,000, member 2,999,999, referrer 1,500,000 and protocol 1,500,000; creator then refunds 90,000,000 with no top-up. The remaining one raw unit backs the member's scaled fraction/index carry. These amounts independently sum to 120,000,000. Later processing cannot earn the canceled balance. A free 10-second purchased gap followed by a 120-token PWYW contribution produces exactly one funded 10-second lot and fully earns all four allocations at its end. The older creator/gift/grant/ownership/refund lifecycle remains covered with current vesting arithmetic and independent total token-custody conservation.

Authoritative web reads now include settled four-purpose balances, fractional reserves, allocation status and explicit single-page history reads pinned to the captured block. Missing accounting is unavailable/partial, never fabricated as zero. A new regression rejects truncated Multicall3 responses; incomplete-but-successful accounting preserves its balances and false completeness marker. The affected read suites pass **13 tests**, typecheck passes, and the refreshed full web suite passes **606 tests / 94 files**. These are local unit/EVM traces, not browser wallet or authentic-fork evidence; beneficiary UI and shared recovery still have their own open tasks.

## US2: recipient attribution and frequency independence (T031–T035)

The rewritten `RewardsTest` passes **13 tests**, including 256 cohort-conservation fuzz runs. Public-entry examples separately account for the tracked 30-token stream and concurrent funding: the halfway join attributes 22.5/7.5 from the original stream plus 7.5/7.5 from the second stream consumed so far. A public expiry/sync/positive-restoration sequence attributes 20/10 from its tracked stream; equal minimum additions restore equal weights without introducing another member allocation. Expected rational values use explicit, small scaled rounding bounds. Other cases cover same-time endpoints, equal-total/different-identity carry flushing, repeated claims, gifts, grant-only joins, retained burned-member credit, maximum supported gross and zero contributions. Dense processing and frequent claims match sparse payout-plus-fraction exactly.

`MembershipModel.sol` no longer contains the obsolete upfront PaymentBook or fee-only/top-up oracle. Its independent four-purpose FundingBook scans absolute service intervals and canceled lots with no production heap, prefix, member-index or library dependency. Accounting and membership invariants use it alongside the separate eager access model. They check earned/unearned/canceled/refunded scaled sums, all four beneficiary liabilities, eligible shares, immutable referrals, permissionless releases, active-rate derivatives between boundaries, failed-exit rollback and every custody bucket. Fractional member credit, carry and dust are included rather than hidden by raw rounding. The original 256-run / 500-depth / fail-on-revert configuration is unchanged. The focused invariant/lifecycle command passes **6 tests across 4 suites**, including all three invariants, with zero handler reverts. The slow oracle intentionally checks drained accounting; bounded intermediate END handling remains covered by the scheduler/public-backlog tests.

Public transitions keep positive member funding backed by an eligible vector; both invariant handlers assert zero unassigned funding. A separately named test deliberately injects an empty vector through `VestingLedgerHarness` and proves all three raw units remain protected and unclaimable. That injection is synthetic and does not imply a reachable tier lifecycle.

The history runner now replays sparse, dense and irregular processing **and different claim schedules**. Its 41-field result includes per-member/referrer claims, enabling exact comparison of raw payouts times Q plus outstanding scaled credit after every economic action. Actual custody is still checked before normalization. Creator entitlement is compared in aggregate: ownership at the moment of each real claim determines its recipient, so changing claim timing across an ownership transfer does not promise identical owner-wallet payouts. Python separately checks the published rational error bound and required transition coverage.

`python3 contracts/test/models/run_vesting_histories.py --histories 16 --actions 100` passes 1,600 economic actions across all three schedules; the linked replay was rerun successfully after ensuring the irregular schedule alternates both referrers. Inputs, 41-field expectations, batch log/hash and schedule metadata are retained under `contracts/deployments/vesting-histories/`. The million-action final corpus remains T076. No production accounting changes were needed for these story assertions; T013–T018 already supply the tested semantics.

## US3: immutable creator choices and execution-time reward weight (T036–T042, T044)

`FactoryAndFeesTest` passes 37 tests, including factory/direct-tier invalid settings, exact fixed-period horizon conversion, immutable custom 2.37× / 731-period terms through ownership changes, and maximum-lifetime-gross rejection before custody/cursor changes while claims/refunds/free access remain available. All prior contract size and creator deployment gas ceilings remain unchanged. The split deployment rehearsal was repeated successfully against disposable Anvil before browser work, with the same exact payload and gas measurements documented above.

The creator now has Some, More, None and Custom controls, independent text summaries and a supplemental chart. Defaults remain Some 1.5×, fixed 1,000 periods and PWYW 10,000 displayed token units. Validation refuses unrepresentable defaults and requires a valid custom input. None canonicalizes its window to zero. Review and published-tier management display permanent curve/rate terms, all-four vesting, permanent weight and creator-sync cutoff. The focused configuration, wizard, registry readback and supporter suites pass **96 tests across five files**, including mismatched actual published curve terms. Typecheck passes after the generated `previewShares` struct was consumed correctly.

Supporter/gift quote reads use the contract at the captured block, with unavailable states and a non-reservation disclosure. Confirmation decodes the supplied successful payment receipt, matches its stable membership ID and actual `SharesIssued` amount, and checks current permanent shares. It does not require the old quote or expiration estimate to remain valid. A wagmi-boundary component test quotes 15,000,000, executes 11,000,000 and accepts later canonical growth to 24,000,000 even when concurrent history shortened access. The confirmed UI reports 11,000,000 actual new shares, separately from cash. No receipt discovery, custom polling or wallet lifecycle machinery was added.

Observed local Chromium controls checks passed: keyboard focus from custom selection to boost/window and then past the supplemental chart; no graph in the accessibility tree; zero axe violations scoped to `.reward-curve-controls`; ordinary wrapper animation and `animation-name: none` under reduced motion; forced-colors distinction and no 390px horizontal overflow. Retained images and axe output are in `evidence/browser/`. These do not claim a human screen-reader session or full-page accessibility completion. The dev server reports the existing optional MetaMask async-storage module warning; no blocking error overlay was present. Publication/payment receipt evidence remains a separate T043 gate.

Calibration now checks **4,101 actual Solidity curve outputs** and six public-tier synchronization/refund cash histories against the independent rational report. Both Forge calibration tests pass; all six member-cash pairs and input hashes are retained under `contracts/deployments/curve-calibration/`. Curve comparisons are exact, and each raw cash entitlement is within one raw unit of the rational calculation. The gradual 1,000-member cash table is still model evidence, explicitly labeled in `calibration.md`; its curve inputs are Solidity-verified.

### Observed curve publication and lifecycle browser journeys (T043, T045–T049)

The dedicated local Chromium journeys pass **2 tests in 1.8 minutes**, against the exact split graph and a mock six-decimal payment asset on disposable chain 31337. The creator publishes None (10000/0), Some (15000/10,000,000,000), More (30000/10,000,000,000) and Custom (23700/20,000,000); the fixed price is 10,000,000. Every pair is read from the actual created tier. A two-period Custom quote is **33,700,000** shares. The test inserts another wallet's one-period purchase before submission, crossing the early window; the supplied successful receipt issues **23,425,000** shares and the page confirms that actual amount. Publication retains the existing three-confirmation rule. Anvil interval mining supplies those blocks; the first transaction-only node run stalled, and a later dev refresh interrupted navigation. Both were test-environment issues; the final repeat passed without changing application confirmation or reconciliation policy.

The PWYW browser journey purchases 10 tokens for **14,997,500** permanent shares, renews free before and after unsynchronized expiry, then synchronizes expiration. After sync, **499,999** raw member-reward units remain claimable. Free renewal and a creator grant restore access with eligibility still false. A **one-raw-unit** contribution adds **one new share**, restores the old **14,997,500** and does not backfill cash. After later expiration and sync while paused, the member collects the retained claim through the normal wallet flow; final weight is **14,997,501**, eligibility false and claimable cash zero. Separate access/weight/eligibility/claim readbacks and the actual changed-quote receipt hash are retained in `evidence/browser/*.json`. Creator grant/sync/pause transitions in this journey use direct authorized local contract calls; browser writes exercise paid/free renewal, positive restoration and the suspended claim. This is not authentic-fork or public-chain evidence.

`ExpiredMembershipSyncTest` and `GrantsAndCapacityTest` pass **23 tests**: creator-only and 1–100 batch bounds; duplicate/burned/unknown-ID behavior; delayed paused sync settling through its real transaction cutoff; no automatic expiry suspension; permanent identity/referrals and durable post-burn claims; free preservation before/after expiry; zero/grant non-restoration; minimum-positive queued restoration; gift restoration and final grant-only revocation. The existing maximum-sync and many-lot gas ceilings are unchanged. The shared membership invariant already passed at 256 runs × 500 depth under T035; no production lifecycle changes were necessary for these assertions.

Supporter and creator-sync UI now explain the cutoff and show permanent shares separately from current eligibility. Actual historical restoration is decoded from `RewardEligibilityUpdated` plus `SharesIssued` in the supplied receipt, not inferred from the older preview. The focused state/component/sync-control suites pass **33 tests**, and typecheck passes. The browser journey verifies those summaries during active but ineligible free/granted access and keeps earned claims reachable after suspension.

## Reserved refunds and current pre-boundary quotes (T050–T053)

The migrated refund and ERC-5643 suites pass **29 tests** (25 refund/ownership/adversarial plus four standards tests). Coverage includes partial and full unused-time refunds after prior claims, mixed paid/free queues, waiting heads, immutable recipient/creator authority, zero owner allowance and no owner funding pull, atomic maximum-gross rejection, unchanged historical shares/cursor, cancellation generations and exact-transfer failures. Canceled funding never earns again. Standard cancellation uses the same reserved-only path and retains AccountingBehind atomicity.

Live-clock review exposed a quote issue: a historical quote was incomplete even one second after processing, although there was no intervening checkpoint. `RefundPreview` now exposes `fundingAsOf` and `projected`. Before the next global boundary, it projects only the selected membership's active head and prefix totals, with constant work and no storage or claimable-balance changes. Any pending global boundary, including one due exactly now, prevents projection and exposes historical funding until processing resumes. **13 PublicVesting tests pass**, including 256 fuzz comparisons of every projected funding/residue component against actual processing, exact-boundary fallback and a funded head waiting behind free access.

The UI labels current projections separately from settled accounting and historical estimates. It refreshes and simulates before submission, rejects changed generations/increased refund limits/incomplete historical funding, and confirms the actual refund and canceled access from the supplied receipt plus canonical generation progress. It does not require later access to stay zero or permanent shares to decrease. The focused web suites pass **38 tests** and typecheck passes. Browser evidence is recorded separately when its final journeys pass.

Generated bindings were regenerated through Wagmi. The projection change was followed by **32 deployment tests** and another fresh local seven-transaction split-graph rehearsal. Exact actual transaction data/gas: ledger **15,958 / 3,493,375**; media factory **10,029 / 2,213,477**; renderer **52,459 / 11,411,670**; preview **882 / 231,950**; tier stores **20,251 / 4,348,445** and **20,251 / 4,346,845**; factory **55,981 / 11,568,012**. Creator tier creation remains a single transaction: **1,540 data bytes / 7,226,927 gas**, within its unchanged 7.5M cap. Exact public mainnet/testnet constructor payload tests remain **56,045 / 56,141 bytes**. All existing payload, runtime, initcode, child-store and gas limits are unchanged. Full source/runtime/immutable graph proof and actual input hex are retained in `contracts/deployments/split-rehearsal/`. The updated local factory is `0x5Ca0be39E8ddC94D0FF64c749872Db00Fa89b596`; earlier browser receipts describe the prior local artifact snapshot. No public deployment, commit or push occurred.

## Durable beneficiary claims (T055–T057)

`ClaimsAndWithdrawalsTest|AdversarialPaymentsAndExitsTest|ReferralsTest` pass **27 tests in three suites**. Claims use only settled balances, preserve fractional credit exactly and permit later positive balances. All four purposes can be collected while paused and while 100 equal-time endpoints remain pending; the claim does not advance that cursor. Burned credentials retain their permanent wallet's claim; unrelated wallets cannot collect it. Zero-value calls perform no token transfer and emit no payout, even with frozen recipients and a reverting token. Two-step ownership moves unclaimed creator proceeds to the new owner. Reverting, false, short, frozen and reentrant token exits preserve protected balances or pay exactly once.

Referral coverage follows the original lot through actual service: no upfront claim, no membership requirement, earlier unattributed gifts never gain a later referrer, sequential and simultaneous streams share one referrer's credit, and claims around rate changes/expiry preserve exact final attribution. Existing implementation satisfied these tests without changing production contracts. This closes contract acceptance for US6; its balance presentation and broader browser journeys remain open.

## Observed refund and beneficiary collection journeys (T054, T058–T060)

The final local Chromium refund/claim suite passes **three journeys in 3.0 minutes**, plus the separate account-discovery journey in **28.4 seconds**, on the projection-enabled split graph. Refund review rejects a previously current quote after another member's END becomes pending, labels historical funding and disables execution. A different wallet processes accounting, all four prior beneficiary portions are collected, and fresh review/refund succeeds without creator allowance. The actual refund is independently recomputed from the execution block's timestamp; interval mining remains enabled, so a nominal 90-token refund is slightly smaller at execution. A second journey queues a positive 120-token PWYW period, a free period and a 60-token period, then partially consumes the first and refunds its remainder plus the queued positive lot. A labeled browser RPC simulation fault leaves generation/cash unchanged; retry succeeds after a new quote. Real failed-token transfer rollback is covered by AdversarialRefundsTest, not claimed from the injected browser fault. Receipt amounts, canceled seconds, unchanged shares/cursor and custody covering protected liabilities are retained in `evidence/browser/refund-*.json`.

The beneficiary journey moves ownership through both normal UI steps while preserving unclaimed creator proceeds. It collects membership rewards, creator proceeds at the new owner and referral proceeds at the original referrer. After each submitted claim, another local caller advances accounting before the browser refreshes. Actual raw payouts are **1,500,000**, **27,900,326** and **300,006**, with respective newly claimable balances **17**, **244** and **2** at the retained readbacks. The browser confirms each actual receipt amount without requiring a zero refreshed balance. The account journey discovers a burned membership with earned rewards, follows its ordinary membership link and successfully claims while eligibility remains false. These are local real-protocol/mock-payment-token flows, not authentic-origin/public deployment evidence.

The UI now labels settled cash and reserved creator/member funding separately, provides accounting timestamps and explains that the future member reserve is shared by whoever becomes eligible, not a personal promise. Positive sub-raw-unit reserves are not displayed as zero. Live announcements describe the actual completed payout; passive accrual summaries have no live region. Receipt reconciliation requires the exact tier and beneficiary (plus token ID for member rewards) and uses the actual positive amount, which can be smaller or larger than a prior preview. Six component cases explicitly reconcile all three beneficiaries with a positive refreshed balance. Focused UI/read/reconciliation tests pass **31 tests**; the full web suite passes **628 tests in 95 files**, with typecheck and lint passing. Retained screenshots of refund confirmation and vesting summaries were visually inspected.

## Full-suite migration checkpoint

The complete Solidity source/test tree now compiles, including fork consumers. Remaining fixture migrations supply the immutable tier-code references to encoded factory constructors and install the linked local graph. Fork-only helpers verify the existing canonical CREATE2 deployer rather than etching a replacement into origin state. Local metadata, identity, capacity and executor suites pass **40 tests**. The buyback invariant retains its **256 runs × 500 calls** and passes with zero handler reverts; its all-purpose protected-liability assertion now uses the new ledger and preserves fractional protocol reserves exactly.

The first full local suite found that the history replay implicitly loaded the last large corpus batch, exceeding its existing one-billion-gas test allowance. The ordinary test now uses a committed deterministic seed-0, 100-action fixture. The corpus runner explicitly supplies its complete generated batch via `BBF_VESTING_HISTORY_INPUT`, keeping the existing corpus workload and its separate harness allowance unchanged. This removes dependence on the previous run's output without changing any product or normal-suite gas/size limit. The standalone history test passes; final full-suite and full-corpus gates remain open.


## US7: permissionless recovery and measured capacity (T061–T069)

The cold full-scale benchmark rejected the original 100-step proposal: even a 100-member all-allocation/distinct-referrer fixture consumed 36,320,662 gas in one combined call. Standard heap hole-sifting replaces redundant swaps without changing heap keys, ordering, lot identities, arithmetic or economics. The supported cap is now **25** in the ledger, tier, router, UI, runner, fixtures and API documents. No size, deployment-gas, chain-gas or per-call acceptance limit was raised.

`BBF_FULL_VESTING_CAPACITY=true FOUNDRY_PROFILE=robinhood forge test --libraries "$BBF_TASK_LINK" --match-contract VestingCapacityTest --code-size-limit 1000000 --gas-limit 100000000000 -vv` passes both release-gate tests. The large test-harness allowance pays for constructing 100,000 separate real purchases in one test; measured protocol calls have a 20M allowance and enforce the existing 15M target. Each call cools the tier, ledger, router, factory, vault and token. Gas includes the actual encoded advance calldata's intrinsic gas, with conservative caller-side overhead.

- 10,000 members, 100,000 funded payments, all four allocations, distinct fixed referrers, identical endpoints, one year idle: **4,000** successful calls, exactly **100,000** completed ENDs, **24,645,895,012** total recovery gas, **11,959,489** peak combined-call gas. Each call includes release and eligible real direct-token burn; external launch identity is explicitly synthetic. Time moves forward by 12 seconds between incomplete calls.
- Subsequent member/referral/creator claims use 127,374 / 86,568 / 73,073 gas; settled zero release uses 12,153. Expiration sync, positive reactivation, purchase and refund then succeed. These are local contract proofs, not market-origin acceptance.
- Canceling a 10,000-payment queue uses **393,291** gas, preserves shares/cursor, increments its generation and removes its sole scheduler node. Full test output: `/tmp/bbf-final-capacity.log`.

Router coverage now includes shared budgets, accounting-only, release-only, buyback-only, exact actual work, no worker payment and input validation. Synthetic adversaries cover stage reverts, gas exhaustion, 64KiB revert data, malformed return data and invalid accounting results. Earlier/later independent work survives, own-stage mutations roll back, and diagnostics stay bounded. Authentic-fork external-failure cases compile but remain unrun pending origin access (T078).

Runner tests retain finite fair tier sweeps, no lifetime-token scans, native viem transaction handling and standing market policy. The focused US7 web set passes **40 tests / 6 files**. Advance reconciliation reads actual per-stage supplied receipt events and fresh canonical state. The website's common control offers caller-selected 0–25 checkpoints, eligible payment-asset buybacks, no-work and partial-success outcomes, plus explicit refresh/retry. Historical reserved funding is separate from settled earnings and carries completeness/as-of data.

Observed Chromium desktop journeys on disposable Anvil chain 31337 pass for purchase, refund and expiration sync. Real contract gifts create 101 due funding checkpoints; six user-requested website advances report **20,20,20,20,20,1**. A settled reward claim succeeds while behind; purchase and sync simulations reject without saved progress; refund shows a historical disabled quote. After recovery, new simulations complete the original action, including a newly read refund quote. Evidence: `browser/recovery-{purchase,refund,sync}.json` and corresponding PNGs. This UI proof used the prior deployed 100-cap graph with requests already within the new cap; final graph repetition belongs to T075/T078. Fixture gifts use an explicit 2M gas allowance to avoid Anvil's timestamp-sensitive default estimate; viem owns receipt polling at a local 200ms interval.

## Numeric documentation and boundary coverage (T071–T072)

`contracts/docs/vesting-and-reward-curves.md` and public NatSpec document C/Q, canonical display conversion, boost/horizon bounds, permanent capacity consumption and asset onboarding considerations. No capacity UI or approval workflow was added. A public-tier test combines maximum C, maximum 10x boost/horizon, uint64 clock ceiling, all four allocations, partial cancellation, capacity exhaustion, all claims and donation separation. It asserts exact scaled conservation and cancellation generation permanence. Existing randomized handlers cover failed exact transfers/reentrancy, all protected purposes, recipients and donations.

After these changes the full non-fork contract suite passes **416 tests / 47 suites**, including 256×500-step invariants; `/tmp/bbf-final-local-contracts.log`. A focused independent oracle corpus of **16 histories × 100 actions × 3 processing/claim schedules** passes; `/tmp/bbf-final-focused-histories.log`. The full 10,000-history acceptance remains T076.


## Final split graph and scoped review checkpoint

A fresh loopback-only Anvil rehearsal on chain 31337 verifies the final separately deployed stores and complete linked graph. Exact transaction data bytes / receipt gas are: ledger **15,814 / 3,462,119**; media factory **10,029 / 2,213,477**; renderer **52,459 / 11,411,670**; preview **882 / 231,950**; store A **20,251 / 4,350,613**; store B **20,251 / 4,349,085**; factory **55,981 / 11,568,024**. The factory has **39,019 bytes** of headroom under the unchanged 95,000-byte payload limit. A creator publishes a tier in one transaction using **1,540 data bytes / 7,230,937 gas**, below the unchanged 7.5M limit. Initcode/runtime/store limits remain unchanged. Retained exact inputs, receipts and graph proofs are in `contracts/deployments/split-rehearsal/`; the factory is `0x10f4E9ae1D7408d34BA8fa8cC9213376e03dec91`. This fixture uses a local mock payment token and is not authentic-origin evidence.

Scoped source review covers permanent issuance/capacity, equal-time scheduling and finalized mutation boundaries, indexed heap removal, cancellation generations, purpose-specific protected liabilities, claims before/after cancellation, fixed referral attribution, router self-call rollback and bounded diagnostics, immutable bytecode reconstruction and factory authority, and frontend receipt/canonical-state reconciliation. No unresolved accounting or authority defect was identified in this review. This is an implementation review, not an independent audit certificate.

Pinned **Slither 0.11.6** passes `--fail-high` on a clean isolated linked build: **0 high, 32 medium, 33 low, 38 informational** findings. Intentional fixed-point division precedes elapsed-time multiplication; protected rounding is tested against the rational model. Solidity-zero-initialized locals, exact checkpoint equality and bounded loops are reviewed in context. The existing canonical-WETH withdrawal balance check raised a high reentrancy-balance warning: both entry points are nonReentrant and the balance comparison deliberately measures exact received ETH. A narrowly scoped annotation documents that guard. A synthetic malicious-unwrap regression attempts `syncDonation` during withdrawal, observes the exact ReentrancyGuard rejection, preserves the 10 ETH inventory and proves repeated sync cannot reuse it. All **8 BuybackAdministration tests pass**; no executable vault behavior changed. Logs: `/tmp/bbf-slither-reviewed.log`, `/tmp/bbf-weth-guard-test.log`.

The private chain-4663 archive preflight passes, and the authentic local Foundry fork suites pass **48 tests, zero failures**. One separate opt-in chain-46630 Safe-deployment test is skipped; it is not part of this mainnet-origin run. The earlier ideal-gross-quarter test expectation was replaced with the exact Q128 release and cancellation-residue rule: a protected one-raw-unit remainder is retained rather than incorrectly requiring empty custody after refund. The combined-stage external failure case passes against the configured origin. Logs and sanitized preflight evidence: `/tmp/bbf-final-authentic-fork-tests.log`, `artifacts/protocol-fork/vesting-final/preflight/report.json`. Full browser lifecycle acceptance remains T078.

The full web unit suite passes **630 tests / 95 files**. Typecheck, lint and formatting pass. Automated accessibility covers keyboard presets/custom input/correction, matching semantic summaries, no chart traversal, default/reduced motion, forced colors, 200% text enlargement and 320px reflow. It found and fixed preset-grid text overflow by allowing content-sized responsive columns. The two dedicated browser tests pass with zero scoped axe violations; artifacts are retained as `browser/vesting-*.png` and `browser/vesting-keyboard-semantics.json`. Named real screen-reader acceptance remains T074 and is not inferred from the accessibility tree.

Final-graph recovery repeat: all **three desktop browser journeys pass in 7.6 minutes** on factory `0x10f4E9ae1D7408d34BA8fa8cC9213376e03dec91`, including keyboard claim/advance activation and zero scoped axe violations on the recovery controls. The retained result is `browser/recovery-final-graph.log`; `browser/recovery-*.json/png` still describe the earlier local graph. The final repeat's detailed Playwright output was cleared by the next suite before its copy completed, so it is not presented as retained receipt evidence. The authentic lifecycle run will retain its own complete artifacts. This closes T070 automated coverage; actual screen-reader acceptance remains T074.

Final deployment-tooling gate: **23 DeploymentScripts tests pass** on isolated final linked artifacts, and the full resumable wrapper suite prints `deploy-protocol wrapper tests: passed`. These supplement the final seven-transaction real local graph and full 100,000-payment capacity measurements above. Clean-room provenance, Safe-wrapper guards, payment-token/buyback administration checks, Solidity formatting and **12 fork CLI lifecycle tests** also pass. Logs: `/tmp/bbf-final-deployment-tests.log`, `/tmp/bbf-final-deploy-shell.log`, `/tmp/bbf-final-fork-cli.log`.

## Final independent-history acceptance (T076)

`python3 test/models/run_vesting_histories.py --histories 10000 --actions 100 --batch-size 32` passes seeds **0–9,999**, each with **100 economic actions**, under sparse, dense and irregular processing/claim schedules. This is one million authored economic actions and three million schedule replays. All required transitions have nonzero coverage, including **71,116 refunds**, **40,611 syncs**, **37,677 free renewals**, **86,137 restorations** and every beneficiary claim. Exact scaled conservation, frequency identity and bounded independent rational attribution pass. The complete coverage, deterministic batch hashes and linked artifact identity are retained in `full-history-summary.json`; batch logs remain in `contracts/deployments/vesting-histories/`. The ledger/accounting implementation did not change during the run; the later vault annotation is comment-only and the later fork refund expectation/evidence fixes do not alter economic execution.

Final binding/consumer audit (T073): Wagmi regeneration and `generate:check` pass on the final linked artifacts. The clean build exposed 18 test-only Forge lint warnings; bounded fixture conversions now use OpenZeppelin SafeCast, mock transfer results are asserted, and deliberate Q128 arithmetic/controlled test-clock comparisons carry narrowly scoped explanations. Production accounting is unchanged. The obsolete protocol-fee-only evidence suite names and refund tuple/top-up expectations were removed. The retained refund verifier now checks actual tier release/refund receipt amounts, received cash, total custody conservation, protected residues and cancellation generation; **7 evidence tests pass**, including missing/wrong receipts and reused/unprotected cash rejection. Asset-dependent accessibility journeys run in the configured-chain browser gate so their currency-normalized summaries have canonical token units.

The full first authentic rehearsal executes **465 contract tests successfully**, with only the separate opt-in testnet Safe test skipped. It stops before broadcast during script verification: copying the 919KB leaf compiler JSON twice into EVM memory exhausts the existing script gas budget. `LinkedVestingDeployment` now uses Foundry's artifact bytecode readers for exactly the same preserved leaf path, derived CREATE2 address and self-patched runtime-hash checks. No runtime contract, payload, protocol gas/size limit or accounting rule changes. The failed local run is retained at `artifacts/protocol-fork/vesting-final-001`; a fresh complete run verifies the fix.

The unconfigured production build passes **82 browser tests**, with **11 intentional environment-specific skips**. Its initial dev-server run is not acceptance: configured localhost state contradicted unavailable-deployment scenarios, and React development Strict Mode restarted the one-use renderer handoff effect. The production run passed those existing cases. Curve accessibility requires configured token units and now runs as part of the authentic-chain gate. Logs: `/tmp/bbf-final-production-build.log`, `/tmp/bbf-final-production-browser.log`.

## Authentic integration discovery (2026-09-10)

`vesting-final-002` passed actual split deployment after the artifact-reader fix, then rejected the strict bootstrap schema because it lacked the new graph fields. The schema now requires all immutable store/deployer/ledger/router references, hashes and bounded creation length; missing-field and oversize tests pass. `vesting-final-003` then passes bootstrap, Safe launch checkpoints, browser fixture, 465 contract tests (one separate opt-in testnet test skipped), 631 web tests and production build. Its browser run retains 58 passes, 15 failures and 98 intentional project/environment skips. All eleven required new-feature desktop journeys pass, including three recovery flows and configured accessibility; overall acceptance remains failed.

Failures reveal stale immediate-payout and linear-share assertions, removed renderer-test getters, exact-versus-Q128 rounding, and local fixture estimates that run out of gas at the next timestamp. One real caller issue is isolated: the runner’s successful `advance` receipt includes release-stage `InsufficientStageGas` (`0x984b2796`), because estimating minimum transaction success can fund accounting alone. T086 records the explicit budget fix and required fresh acceptance. No contract accounting or deployment bytecode changes are needed. Retained unsuccessful runs remain under `artifacts/protocol-fork/vesting-final-00{1,2,3}`.

T086 isolated gas regression: the same local tier/cursor snapshot is executed first with native estimated gas **1,210,613**, then restored and executed with the explicit **15,000,000** budget. The first receipt succeeds for accounting but reports release `InsufficientStageGas` and zero released tiers. The second successfully releases one tier, consuming only **314,037 gas**. Exact receipts/events are retained in `advance-gas-reproduction.json`. The outer snapshot is restored afterward. This is a synthetic local payment-token graph, not authentic-origin evidence. Focused budget/readiness/native-request regressions pass **21 tests / 3 files**; typecheck, lint and formatting pass. The fresh complete authentic run is `vesting-final-004`.

`vesting-final-004` repeats the clean deployment, 465 passing contract tests (one unrelated opt-in skip), **632 web tests**, production build and authentic browser suite. Browser results are **68 passed, 5 failed, 98 intentional skips**. All three backlog recovery journeys pass again. Both independent runner callers now have successful `AdvanceCompleted` receipts with one released tier and no `StageFailed`, and actual token burns reconcile. The five remaining failures are test selectors matching the separate funding summary, the AMD fixture resetting its selected account on reload, and an immediate viewport-width read. The reflow probe reproduces 438px immediately after resizing and exactly 320px after two layout frames in four runs; the same strict width assertion now uses Playwright's retry mechanism. No application layout/economic change follows from this timing race. The targeted follow-up uses a private saved/restored desktop baseline and is explicitly labeled `restored targeted regression; not fresh complete acceptance` in `artifacts/protocol-fork/vesting-final-004-focused/scope.json`. A clean full repeat remains required.

The targeted restored run passes publication at 100% protocol allocation, both AMD display/claim/refund flows, and both accessibility cases. Its creator fixture exposed Anvil’s saved-state clock behavior: loaded storage had an accounted-through timestamp in 2034 while the node mined in 2026. The ledger correctly rejected the backwards clock. The repository review-restore helper now advances to `max(saved timestamp, current timestamp) + 1` before serving; **14 lifecycle guard tests pass**, including missing/malformed clock rejection. The subsequent creator advance has an actual successful receipt using **708,987 gas**; its UI assertion now uses the same 45-second native receipt/reconciliation allowance as the other transaction tests instead of Playwright’s five-second default. Restored checks remain outside fresh acceptance.

## Final authentic execution and independent reconciliation (T077, T078, T086)

The fresh `vesting-final-005` lifecycle passes **465 contract tests**, **632 web tests**, the production build and **73 browser tests** in 20.7 minutes. There are zero failed or flaky browser cases and 98 intentional project/environment skips; all eleven mandatory vesting/curve desktop journeys execute and pass. The only contract skip is the separate opt-in chain-46630 Safe test. The source chain is **4663**, block **58,083,838**, hash `0xbed1732da1a4301c9f3ea9c5eafbdf76f80e8b4afade3e3c0fe66d41f794ea6c`; execution is disposable local chain **31337**. Preflight and export both record dirty base commit `d67bf93a9bb8eb95f181cb42520e5538d6052b20` with identical source snapshot SHA-256 `7ff6ea77557ff936dc444dee6efa0116aeae294b88540454d73be424ef1d535e`. The linked ledger runtime exactly matches the final 10,000-history corpus: `0xb0e7da714f3a81f00e99545a54ada2488b79bfc91d420c5a72db8a754c2be18c`.

The original final verifier fails after successful execution/export because it rejects a current projected refund and still expects a scheduled multi-sweep runner. The approved standing operating model and existing runner use finite one-shot sweeps. The corrected verifier accepts a projection only when its funding timestamp equals the access timestamp and is no older than settled accounting, while retaining all four funding-purpose checks, actual refund/release receipts, received cash, custody conservation, protected residues and cancellation generation. Runner verification requires two distinct valid callers, zero remaining gas for the removed caller, actual replacement processing within 60 seconds, additional released funding, exactly one complete bounded sweep per caller and unique ordered tier visits. Burn receipts, destroyed token supply, inventory, source/runtime identities, artifact hashes and gas/size gates remain enforced. **Eight verifier tests pass**, including rejection of historical/mismatched projections, incomplete/repeated/duplicate sweeps, identical callers and timing/gas-removal failures.

`bun scripts/protocol-fork/verify-evidence.ts artifacts/protocol-fork/vesting-final-005` independently passes on the unchanged retained execution artifacts. At that verification, only the verifier and its unit tests had changed after execution; no application, contract, runner, receipt, preflight, source inventory or artifact index was changed to obtain that result. The generated `reconciliation.json` reports `runLocalPassed: true`; the original `lifecycle.json` failure and `verify-evidence.log` remain intact. The previous feature's second-fresh-run reproduction certificate remains `completeAcceptance: false` / not-run; this is not a claim of that separate release certificate. The compact [final-local-acceptance.json](final-local-acceptance.json) retains the corrected verifier hash and exact evidence boundaries. The later, separately verified presentation correction is recorded below.

Actual signed transaction sizes from this authentic execution are:

| Deployment | Input bytes | Signed bytes | Actual gas |
|---|---:|---:|---:|
| Vesting ledger | 15,814 | 15,921 | 3,462,119 |
| Tier code store A | 20,251 | 20,358 | 4,350,613 |
| Tier code store B | 20,251 | 20,358 | 4,349,085 |
| Media store factory | 9,997 | 10,084 | 2,208,937 |
| Renderer | 52,427 | 52,514 | 11,386,163 |
| Preview harness | 850 | 937 | 231,032 |
| Factory, vault, executor, router, tier deployer and executor store | 55,949 | **56,037** | 13,832,607 |

The factory has **38,963 signed bytes of headroom** under the unchanged 95,000-byte limit. This authentic fixture uses CREATE2 for the ledger/tier stores and direct CREATE for the other protocol roots; the separate public-wrapper rehearsal above verifies all seven canonical CREATE2 payloads and interruption/resume behavior. Every retained transaction has its exact input and signed serialized size, and the complete graph includes raw stores, immutable bindings, linked library, factory children and executor. Creator publication remains a single transaction under the unchanged 7.5M cap, as separately measured on the final split graph and exercised by the authentic publication journeys. No size or gas gate was raised.

Final checks after the verifier correction: **633 web tests in 95 files**, typecheck, lint, formatting, Wagmi `generate:check`, frozen-lockfile dependency check (993 installs, no changes), final source/build starter/secret-pattern scan and `git diff --check` pass. The full contract, pinned Slither, wrapper, lifecycle, history and capacity results above cover the unchanged execution code. The `verify-local.sh` component checks were run individually and the full authentic lifecycle was run separately; the aggregate shell wrapper itself is not claimed as a completed invocation. Logs remain under `/tmp/bbf-final-*` and execution artifacts under `artifacts/protocol-fork/vesting-final-005/`.

**Remaining acceptance:** T074's actual named screen-reader session is not complete. Keyboard, semantic, axe, visual preference, zoom and reflow browser checks pass but do not substitute for assistive-technology observation. Permission to briefly enable and then restore macOS VoiceOver is pending. No public deployment, commit or push occurred.

## Convergence and final presentation regression (T080, T088)

The required `speckit-converge` workflow assessed 54 functional requirements, 12 success criteria, 38 story acceptance scenarios, six implementation stages and five constitutional principles. It found two partial, medium-severity gaps: custom-window labels inferred a preset from the boost alone (FR-015/038/053), and named screen-reader evidence remains absent (SC-012). It appended T088 and T089 under Phase 11 without changing existing tasks, source or feature intent during convergence. No extension hooks are configured. No additional accounting, deployment, authority or transaction-lifecycle gap was identified; this is scoped internal review, not an independent audit certificate.

Resuming implementation, the summary now classifies the complete boost/window pair. Fixed-price defaults use 1,000 periods; PWYW defaults use 10,000 displayed units converted by the existing token helper. Custom windows paired with Some/More boosts retain a Custom summary at review and in tier management. Without token metadata, PWYW summaries do not claim a known preset window. Eight component cases cover None, all preset/custom combinations and scaled PWYW display units. The wizard regression also exposed custom fields disappearing on the intermediate `1` while typing `1.5` after returning from review. Editing a custom field now retains that editor, keeping keyboard focus and the previously entered window.

The full suite passes **641 tests in 96 files**; the focused wizard/summary suites pass **28 tests**. Production build, typecheck, lint and formatting pass. Both dedicated production-browser accessibility journeys pass against the existing final local mock-asset split graph in **2.2 seconds**, including a return from review, sequential keyboard entry of `1.5`, preserved focus/window, and the final `Custom / 1.5× / 42 purchased periods` review. Motion, forced colors, 200% text and 320px reflow also pass. Evidence: `browser/convergence/report.json`, `browser/convergence/browser.log`, `browser/convergence/build.log` and screenshots/semantics under `browser/convergence/test-results/`. This focused local presentation proof supplements the earlier authentic run; it is not a second fresh authentic execution. Contract, deployment, runner and accounting sources remain identical to that run, so the full history/capacity/deployment results still apply.

T080 and T088 are complete. T074 and its convergence evidence check T089 represent one remaining acceptance gap: the named screen-reader session. They remain open pending permission. The feature is not reported as fully accepted or ready for public deployment.
